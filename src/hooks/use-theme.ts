// src/hooks/use-theme.ts
/**
 * Raccourci pour accéder au thème actif depuis n'importe quel composant.
 * Usage : const { colors, name, setTheme } = useTheme();
 */
export { useTheme } from '@/context/ThemeContext';